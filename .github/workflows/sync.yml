name: Sync Fork

on:
  schedule:
  - cron: '*/2 * * * *'

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
    - uses: TG908/fork-sync@releases/v1.1.3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        owner: llvm
        base: master
        head: master
        
